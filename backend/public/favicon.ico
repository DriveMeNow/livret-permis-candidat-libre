app.use(cookieParser());
app.use(express.json());

app.use(csrf({
  cookie: {
    httpOnly: true,
    secure: true,        // uniquement en HTTPS
    sameSite: 'Strict'   // ou 'Lax' selon tes besoins
  }
}));
